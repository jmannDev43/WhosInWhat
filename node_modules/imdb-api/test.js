var imdb = require('./lib/imdb.js');

imdb.get("Ouran High School Host Club", function(err, show) {
	console.log(show);
	show.episodes(console.log);
	show.episodes(function(err, eps) {
		console.log(show._episodes);
	});
});
